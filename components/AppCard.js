import Link from 'next/link';
function compact(n=0){ return n>=1000000?`${(n/1000000).toFixed(n>=10000000?0:1)}M`:n>=1000?`${Math.round(n/1000)}K`:String(n); }
export default function AppCard({app}){ return <Link href={`/app/${app.slug}`} className="app-card"><div className="app-card-top"><img src={app.icon_url||'/demo/icons/default.svg'} alt="" /><div><h3>{app.title}</h3><p>{app.category?.name||'Android App'}</p></div></div><div className="app-card-meta"><span>★ {Number(app.rating||0).toFixed(1)}</span><span>{compact(app.downloads)} downloads</span></div><div className="tag-row"><span className="pill">{app.app_type==='game'?'GAME':'APP'}</span>{app.mod_info&&<span className="pill green">MOD</span>}</div></Link> }
