'use client';import {useRouter} from 'next/navigation';export default function ContactsAdmin({rows}){const r=useRouter();async function mark(id){await fetch('/api/admin/contacts',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,is_read:true})});r.refresh()}return <div className="admin-card"><table className="admin-table"><thead><tr><th>Name</th><th>Email</th><th className="hide-mobile">Subject / Message</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.name}</td><td><a className="action-link" href={`mailto:${x.email}`}>{x.email}</a></td><td className="hide-mobile"><strong>{x.subject||'Contact form'}</strong><br/><span className="muted">{x.message}</span></td><td>{x.is_read?'Read':<button className="small-btn" onClick={()=>mark(x.id)}>Mark read</button>}</td></tr>)}</tbody></table>{!rows.length&&<div className="empty">No contact messages yet.</div>}</div>}
