'use client';import {useState} from 'react';
export default function ContactForm(){const[f,setF]=useState({name:'',email:'',subject:'',message:''});const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);const set=(k,v)=>setF(x=>({...x,[k]:v}));async function submit(e){e.preventDefault();setBusy(true);setMsg('');const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});const j=await r.json();setBusy(false);if(r.ok){setMsg('Thanks. Your message has been sent.');setF({name:'',email:'',subject:'',message:''})}else setMsg(j.error||'Unable to send message.')}return <form className="contact-form" onSubmit={submit}>{msg&&<div className="notice">{msg}</div>}<label>Name<input required value={f.name} onChange={e=>set('name',e.target.value)}/></label><label>Email<input required type="email" value={f.email} onChange={e=>set('email',e.target.value)}/></label><label>Subject<input value={f.subject} onChange={e=>set('subject',e.target.value)}/></label><label>Message<textarea required rows="7" value={f.message} onChange={e=>set('message',e.target.value)}/></label><button disabled={busy} className="primary-btn">{busy?'Sending…':'Send Message'}</button></form>}
