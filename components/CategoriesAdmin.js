'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function CategoriesAdmin({categories}){const r=useRouter();const[f,setF]=useState({name:'',slug:'',description:''});async function add(e){e.preventDefault();const res=await fetch('/api/admin/categories',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});const j=await res.json();if(!res.ok)return alert(j.error);setF({name:'',slug:'',description:''});r.refresh()}async function del(id){if(!confirm('Delete this category? Apps using it will become uncategorised.'))return;const res=await fetch('/api/admin/categories',{method:'DELETE',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});const j=await res.json();if(!res.ok)return alert(j.error);r.refresh()}return <div className="contact-grid"><form className="admin-card settings-form" onSubmit={add}><h2>Add Category</h2><label>Name<input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>Slug<input value={f.slug} onChange={e=>setF({...f,slug:e.target.value})} placeholder="auto-from-name"/></label><label>Description<textarea rows="4" value={f.description} onChange={e=>setF({...f,description:e.target.value})}/></label><button className="primary-btn">Add Category</button></form><div className="admin-card"><h2>Categories</h2><table className="admin-table"><tbody>{categories.map(c=><tr key={c.id}><td><strong>{c.name}</strong><br/><span className="muted">/{c.slug}</span></td><td><button className="danger-link" onClick={()=>del(c.id)}>Delete</button></td></tr>)}</tbody></table></div></div>}
