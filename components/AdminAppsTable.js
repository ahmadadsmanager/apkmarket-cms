'use client';import {useRouter} from 'next/navigation';
export default function AdminAppsTable({apps}){const r=useRouter();async function del(id,title){if(!confirm(`Delete ${title}?`))return;const res=await fetch('/api/admin/apps',{method:'DELETE',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});const j=await res.json();if(!res.ok)return alert(j.error);r.refresh()}return <div className="admin-card"><table className="admin-table"><thead><tr><th>App</th><th>Status</th><th className="hide-mobile">Category</th><th className="hide-mobile">Updated</th><th>Actions</th></tr></thead><tbody>{apps.map(a=><tr key={a.id}><td><div className="row-app"><img src={a.icon_url||'/demo/icons/default.svg'} alt=""/><div><strong>{a.title}</strong><br/><span className="muted">{a.developer}</span></div></div></td><td>{a.status}</td><td className="hide-mobile">{a.category?.name||'—'}</td><td className="hide-mobile">{a.updated_at?new Date(a.updated_at).toLocaleDateString():'Demo'}</td><td><a className="action-link" href={`/admin/apps/edit/${a.id}`}>Edit</a> &nbsp; <button className="danger-link" onClick={()=>del(a.id,a.title)}>Delete</button></td></tr>)}</tbody></table></div>}
