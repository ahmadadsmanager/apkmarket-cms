import Header from '@/components/Header';import Footer from '@/components/Footer';import AppCard from '@/components/AppCard';import {getApps} from '@/lib/db';
export const dynamic='force-dynamic';export const metadata={title:'Games'};export default async function Page(){const apps=await getApps({type:'game',limit:100});return <><Header/><main className="container page-shell"><div className="page-head"><span className="eyebrow dark">Play more</span><h1>Android Games</h1><p>Discover game platforms, new releases and popular Android titles.</p></div><div className="app-grid">{apps.map(a=><AppCard app={a} key={a.id}/>)}</div></main><Footer/></>}
