import {protectAdmin} from '@/lib/admin-page';import {configured,select} from '@/lib/db';import ContactsAdmin from '@/components/ContactsAdmin';export default async function Page(){await protectAdmin();let rows=[];if(configured)rows=await select('contacts','select=*&order=created_at.desc');return <><div className="admin-top"><div><h1>Contact Messages</h1><p>Enquiries submitted through the website.</p></div></div><ContactsAdmin rows={rows}/></>}
