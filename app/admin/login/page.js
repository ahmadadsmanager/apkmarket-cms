'use client';import {useState} from 'react';
export default function Login(){const[e,setE]=useState('');const[p,setP]=useState('');const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function submit(x){x.preventDefault();setBusy(true);const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:e,password:p})});const j=await r.json();setBusy(false);if(r.ok)location.href='/admin';else setMsg(j.error||'Login failed')}return <div className="login-shell"><div className="login-card"><div className="brand"><span className="brand-mark">A</span><span>APKMarket CMS</span></div><h1>Admin Login</h1><p>Use the ADMIN_EMAIL and ADMIN_PASSWORD configured in your hosting environment.</p>{msg&&<div className="notice">{msg}</div>}<form onSubmit={submit}><label>Email<input type="email" required value={e} onChange={x=>setE(x.target.value)}/></label><label>Password<input type="password" required value={p} onChange={x=>setP(x.target.value)}/></label><button disabled={busy} className="primary-btn">{busy?'Signing in…':'Sign in'}</button></form></div></div>}
