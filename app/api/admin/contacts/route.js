import {NextResponse} from 'next/server';import {requireAdmin} from '@/lib/admin-api';import {update,configured} from '@/lib/db';export async function PUT(req){try{await requireAdmin();if(!configured)throw new Error('Configure MySQL first.');const b=await req.json();await update('contacts',b.id,{is_read:Boolean(b.is_read)});return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e.message},{status:400})}}
