import {NextResponse} from 'next/server';import {requireAdmin} from '@/lib/admin-api';import {getSettings} from '@/lib/db';import {sendContactNotification} from '@/lib/mail';export const runtime='nodejs';export async function POST(){try{await requireAdmin();const s=await getSettings();const result=await sendContactNotification(s,{name:'CMS Test',email:s.notification_email,subject:'Test email',message:'Your APKMarket CMS email settings are working.'});if(result?.skipped)throw new Error('Complete notification email, SMTP user and app password first.');return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e.message},{status:400})}}
