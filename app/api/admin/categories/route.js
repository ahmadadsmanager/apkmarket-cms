import {NextResponse} from 'next/server';import {requireAdmin,safeSlug} from '@/lib/admin-api';import {insert,remove,configured} from '@/lib/db';
export async function POST(req){try{await requireAdmin();if(!configured)throw new Error('Configure MySQL first.');const b=await req.json();if(!b.name)throw new Error('Category name is required');const rows=await insert('categories',{name:b.name.trim(),slug:safeSlug(b.slug||b.name),description:b.description||''});return NextResponse.json(rows?.[0]||{ok:true})}catch(e){return NextResponse.json({error:e.message},{status:400})}}
export async function DELETE(req){try{await requireAdmin();if(!configured)throw new Error('Configure MySQL first.');const b=await req.json();await remove('categories',b.id);return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e.message},{status:400})}}
