import {NextResponse} from 'next/server';import {requireAdmin,cleanApp} from '@/lib/admin-api';import {insert,update,remove,configured} from '@/lib/db';
export async function POST(req){try{await requireAdmin();if(!configured)throw new Error('Configure MySQL before saving CMS changes.');const app=cleanApp(await req.json());if(!app.title)throw new Error('Title is required');const rows=await insert('apps',{...app,created_at:new Date().toISOString()});return NextResponse.json(rows?.[0]||{ok:true})}catch(e){return NextResponse.json({error:e.message},{status:e.message==='UNAUTHORIZED'?401:400})}}
export async function PUT(req){try{await requireAdmin();if(!configured)throw new Error('Configure MySQL before saving CMS changes.');const b=await req.json();const app=cleanApp(b);const rows=await update('apps',b.id,app);return NextResponse.json(rows?.[0]||{ok:true})}catch(e){return NextResponse.json({error:e.message},{status:e.message==='UNAUTHORIZED'?401:400})}}
export async function DELETE(req){try{await requireAdmin();if(!configured)throw new Error('Configure MySQL before saving CMS changes.');const b=await req.json();await remove('apps',b.id);return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e.message},{status:e.message==='UNAUTHORIZED'?401:400})}}
