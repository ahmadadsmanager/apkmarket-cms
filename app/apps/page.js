import Header from '@/components/Header';import Footer from '@/components/Footer';import AppCard from '@/components/AppCard';import {getApps} from '@/lib/db';
export const dynamic='force-dynamic';export const metadata={title:'Apps'};export default async function Page(){const apps=await getApps({type:'app',limit:100});return <><Header/><main className="container page-shell"><div className="page-head"><span className="eyebrow dark">Browse library</span><h1>Android Apps</h1><p>Popular apps, useful tools and fresh updates in one clean directory.</p></div><div className="app-grid">{apps.map(a=><AppCard app={a} key={a.id}/>)}</div></main><Footer/></>}
